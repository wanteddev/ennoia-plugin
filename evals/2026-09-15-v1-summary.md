# 2026-09-15 · V1 후보 검증 요약

54개 동일 input-only 합성 case의 frozen SHA-256은 `f7915c0f68d5031b279d899df042e702f29e6739fb252c877d2f0aa2b6c2388d`입니다. 기존 workflow 12개·표시 11개·dogfooding 16개·신규 server-aware 15개를 합쳤고 기대 판정 rubric은 입력에서 분리했습니다. 독립 blind 행동 판정은 모델이 제안한 다음 행동의 평가이며 native host 도구 호출·OAuth·업무 성공이나 token/latency 측정이 아닙니다. 수치는 root가 비식별 판정에서 확인한 raw 결과로, source 계약 검토가 raw 점수를 바꾸지 않습니다.

| 후보 Plugin source | baseline raw | candidate raw | 유효 판정의 제한 |
| --- | --- | --- | --- |
| `a021de858d509ee7bff231b6210fbad440884538` | 50 pass, 4 partial | 52 pass, 1 partial, 1 fail | 사용 불가 tool 상황에서 Skill 이름을 Plugin ID로 추측했고, 이미 완료된 전체 답변을 다시 읽는 제안이 남음 |
| `0963ae4ff0e2ca74dac6fdfe7bef8cea2753067b` | 50 pass, 4 partial | 46 pass, 6 partial, 2 fail | 독립 판정자가 canonical revision 입력을 재확인한 후의 점수; stale validation operation/target 추측과 usage 미발견 인자 추측 등 회귀가 남음 |
| `70aaa645183f724a5fb2ee4f15a92830bc32c2f0` | 49 pass, 5 partial | 50 pass, 3 partial, 1 fail | raw fail은 deploy retry의 명시적인 `keep_previous_running=true`. Partial 3건은 중복 RAG list, 구 schema welcome flag 필수 취급, 빈 `{}` schema를 no-args로 오인한 제안 |

첫 두 불리한 유효 비교를 삭제하거나 최종 후보 점수로 대체하지 않습니다. Source에 도달할 수 없는 fixture와 rubric 문구가 입력에 섞인 초기 예비 판정은 입력 타당성이 깨져 제외했습니다. 유효 결과의 `fail`/`partial`도 공개한 채 수정 source와 같은 frozen 입력으로 재실행했습니다. Baseline도 이미 맞춘 case는 개선 수에 더하지 않습니다.

마지막 raw fail에 대한 별도 source 판정에서 M `keep_previous_running`의 기본값은 true이고 unset 값도 `model_dump(exclude_none=True)`의 정규화된 HTTP payload에 포함됩니다. O 요청 schema 기본값과 deployment request hash도 동일 bool을 사용합니다. Root의 순수 모델 probe에서 명시적인 true와 생략 입력의 transport payload가 동일했고 독립 최종 reviewer도 이 case를 source상 pass로 확인했습니다. 이 source-qualified 후보는 **51 pass/3 partial/0 fail, source로 확인된 unsafe 제안 0건**입니다. Raw blind 후보 **50 pass/3 partial/1 fail은 변경하지 않습니다**. Source 판정은 forward 평가 1회를 더 실행한 점수가 아닙니다.

중요한 F5가 남습니다. 구 schema App의 관측된 여섯 설정이 모두 완전해도 새 `welcome_message_enabled`를 무조건 요구하는 분기가 수정 경로를 막습니다. 따라서 source-qualified 0 fail을 구 계약 완료 또는 전체 release 통과로 해석하지 않습니다. 세 partial의 불필요한 RAG 재조회·구 schema welcome flag 판단·빈 schema의 인자 판단도 동작 품질 제한으로 남습니다.

[`host 96-case 기록`](results/2026-09-15-host-cases.json)은 실제 Codex CLI 후보 Git 설치와 새 Skill 로딩 두 건만 `pass`, 나머지 94건은 `not_tested`입니다. [`후보 native CLI 읽기`](results/2026-09-15-codex-cli-native-read.json) 두 건은 인증된 현 세션 읽기 성공이나 연결 source unknown이고, [`기존 Codex App 읽기`](results/2026-09-15-current-session-read.json) 두 건은 1.0.2 이전 세션입니다. Plugin-only OAuth, App/Claude UI, 직접 App와 SuperApp 업무, 쓰기 cleanup, 새 server initialize/schema, 모델 token·응답 크기·비교 성능은 별도 미확인 gate입니다. 후보 CLI 세션의 88,074ms는 startup/다른 Plugin 포함 단일 결과라 성능 쌍이 아닙니다. Release 판단에는 같은 server revision·모델·입력·cache 조건의 실제 host 업무 결과가 필요합니다.
